/*
package com.office.monitoring.withdrawnUser;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface WithdrawnUserRepository extends JpaRepository<WithdrawnUser, Long> {
    // JpaRepository를 상속받으면 기본적인 CRUD(save, findById 등) 메서드가 자동 제공됩니다.
    // 특별한 조회 로직이 필요하지 않다면 내용 없이 비워두시면 됩니다.
}*/
